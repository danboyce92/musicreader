

const scoreLogic = () => {
    let initialScore = 500;

    


// useInterval lowers amount up to 5 seconds
// it should start when timer is running
// it should stop when timer stops
// it should reset everytime
// it should add what's left to total score if answer is right
// if there is 0 left add 100
}

